The server.py is a simple prototype for central server which routes the UDP messages between two computers over the internet.

The local_chat_proto.py is a simple proof of concept application which uses the ncl to allow the users to connect by UDP over internet and chat.
